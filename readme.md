# Istio mtls & Vault PKI Engine example


# This example:

- demonstrates two servers (written in python) communicating and can be used to test mtls in istio & using Vault PKI as a certificate CA

&nbsp;
&nbsp;


---